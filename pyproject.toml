[project]
name = "earnings-call-env"
version = "0.1.0"
description = "OpenEnv Earnings Call Q&A Environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"